//! Audit Service (optional) — consumes saga events into `audit_events` (Task 10).
//! Skeleton entrypoint for now (Task 2).

fn main() {
    println!("audit-service: skeleton (not yet implemented)");
}
